class Contato {

    constructor(cliente, assunto, mensagem, icon) {
        this.cliente = cliente
        this.assunto = assunto
        this.mensagem = mensagem
        this.icon = icon
        
    }
}

module.exports = Contato